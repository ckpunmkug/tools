#!/bin/sh
FILE=`/usr/bin/realpath $0`
UID=`/usr/bin/id -r -u`
if test $UID -eq 0
then
	export DISPLAY="${1}"
	export XAUTHORITY="${2}"
	ip netns exec isolator /usr/bin/dbus-run-session /usr/bin/xfce4-terminal
else
	pkexec ${FILE} ${DISPLAY} ${XAUTHORITY}
fi
